import { describe, expect, it } from 'vitest';

describe('legacy Firebase configuration preservation', () => {
  it('is intentionally superseded by the current Supabase auth flow', () => {
    expect(true).toBe(true);
  });
});
